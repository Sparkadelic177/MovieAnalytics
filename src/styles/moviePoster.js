import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    posterContainer: {
    },
    posterImage: {
        height: 100,
        width: 100,
        resizeMode:'cover'
    },
    posterText: {
        color: 'black'
    }
});
