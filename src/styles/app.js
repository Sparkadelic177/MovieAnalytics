import {StyleSheet} from 'react-native'

export default StyleSheet.create({
    container: {
        marginTop: 50,
    },
    searchContainer: {
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center'
    },
    searchButton: {
        width: '20%'
    },
    input: {
        width: '70%',
        height: '40%'
    }
});
